{
  "name": "codeex",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.15.4",
    "node-fetch": "^1.7.2",
    "node-spotify-api": "^1.0.6",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && babel src-srv -d server",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server-build": "babel src-srv -d server"
  }
}
